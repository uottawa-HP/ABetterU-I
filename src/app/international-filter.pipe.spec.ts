import { InternationalFilterPipe } from './international-filter.pipe';

describe('InternationalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InternationalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
