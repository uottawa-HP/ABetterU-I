import { MentalHealthFilterPipe } from './mental-health-filter.pipe';

describe('MentalHealthFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MentalHealthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
