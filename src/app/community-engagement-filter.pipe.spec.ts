import { CommunityEngagementFilterPipe } from './community-engagement-filter.pipe';

describe('CommunityEngagementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommunityEngagementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
