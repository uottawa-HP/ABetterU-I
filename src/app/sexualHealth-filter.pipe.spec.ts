import { SexualHealthFilterPipe } from './sexualHealth-filter.pipe';


describe('CheckboxFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SexualHealthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
