import { FavouritesPipe } from './favourites.pipe';

describe('FavouritesPipe', () => {
  it('create an instance', () => {
    const pipe = new FavouritesPipe();
    expect(pipe).toBeTruthy();
  });
});
