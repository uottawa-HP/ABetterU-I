import { AcademicsFilterPipe } from './academics-filter.pipe';

describe('AcademicsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AcademicsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
