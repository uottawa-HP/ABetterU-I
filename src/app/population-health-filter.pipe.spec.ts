import { PopulationHealthFilterPipe } from './population-health-filter.pipe';

describe('PopulationHealthFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PopulationHealthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
