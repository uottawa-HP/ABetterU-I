import { NutritionFilterPipe } from './nutrition-filter.pipe';

describe('NutritionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NutritionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
