import { AffiliationPipe } from './affiliation.pipe';

describe('AffiliationPipe', () => {
  it('create an instance', () => {
    const pipe = new AffiliationPipe();
    expect(pipe).toBeTruthy();
  });
});
