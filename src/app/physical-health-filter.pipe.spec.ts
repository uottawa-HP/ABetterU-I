import { PhysicalHealthFilterPipe } from './physical-health-filter.pipe';

describe('PhysicalHealthFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhysicalHealthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
