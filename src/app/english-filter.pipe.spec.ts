import { EnglishFilterPipe } from './english-filter.pipe';

describe('EnglishFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnglishFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
