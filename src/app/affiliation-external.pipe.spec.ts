import { AffiliationExternalPipe } from './affiliation-external.pipe';

describe('AffiliationExternalPipe', () => {
  it('create an instance', () => {
    const pipe = new AffiliationExternalPipe();
    expect(pipe).toBeTruthy();
  });
});
